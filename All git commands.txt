git config --list	// see configuration list

git config --global user.email "abh********@gmail.com"  // set user email

git config --global user.name "abhi"     // set user name
 
touch file1.txt    // create a file file1.txt

git status

git init 		// Initialize a git repo 

git add .

git commit -m "Message"

git remote add origin url  	// add remote origin url

git remote

git push -u origin master/main

git log			// See last 3 commits as log in detaile

git log --online	// In short

git log --grep="only" 	// shows all commits whcih contains 'only' in commit message

git log --since="dd/mm/yyyy"	// shows all commits since dd/mm/yyyy

git log --until="dd/mm/yyyy"	// shows all commits until dd/mm/yyyy
	